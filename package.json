{
  "name": "structured-runner",
  "version": "0.1.1",
  "private": true,
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "build": "tsc -b packages/core",
    "test": "npm --prefix packages/core test",
    "example:basic": "npm run build && tsx examples/basic-node/src/index.ts"
  },
  "dependencies": {
    "@structured-runner/core": "file:./packages/core",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "tsx": "^4.16.2"
  }
}